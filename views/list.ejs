<%- include("header")  -%>
    <div class="box" id="heading">
    <h1><%= listTitle %> </h1>
    </div>

<div class="box">
     <%   dd.forEach(function(i){ %>
      <form action="/delete" method="post">
         <div class="item">   
          <input type="checkbox" name="checkBox" value="<%= i._id %>" onchange="this.form.submit()" >
           <p> <%= i.name %> </p>
        </div>
        <input type="hidden" id="custId" name="titleval" value="<%= listTitle %>">
      </form>
      <%  }); %> 
    
   <form class="item" action="/" method="post">
    <input type="text" name="newitem" placeholder="new Item" autocomplete="off">
    <button type="submit" name="named" value="<%= listTitle %>">+</button>
   </form>

</div>
<%- include("footer") -%>